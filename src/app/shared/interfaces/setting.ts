/**
 * Represents a setting of the app.
 */
export interface Setting {
  /**
   * Setting's id.
   */
  id: number;
  /**
   * Setting's name.
   */
  name: string;
  /**
   * Setting's value.
   */
  value: string;
}
